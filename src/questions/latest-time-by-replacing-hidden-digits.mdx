---
id: 1736
title: "Latest Time by Replacing Hidden Digits"
difficulty: "קל"
source: "LeetCode"
companies:
  - Checkpoint
videoUrl: "https://www.youtube.com/embed/c3mBmLrptdM"
---

# Latest Time by Replacing Hidden Digits

## תיאור השאלה

You are given a string `time` in the form `hh:mm`, where some digits are hidden (represented by `?`). Return the latest valid time you can get by replacing the hidden digits.

## פיתרון

Fill each unknown digit greedily from left to right with the largest value that keeps the time valid.

```javascript
function maximumTime(time) {
  const chars = time.split("");
  if (chars[0] === "?") {
    chars[0] = chars[1] === "?" || chars[1] <= "3" ? "2" : "1";
  }
  if (chars[1] === "?") {
    chars[1] = chars[0] === "2" ? "3" : "9";
  }
  if (chars[3] === "?") {
    chars[3] = "5";
  }
  if (chars[4] === "?") {
    chars[4] = "9";
  }
  return chars.join("");
}
```

```python
def maximumTime(time: str) -> str:
    chars = list(time)
    if chars[0] == "?":
        chars[0] = "2" if chars[1] in {"?", "0", "1", "2", "3"} else "1"
    if chars[1] == "?":
        chars[1] = "3" if chars[0] == "2" else "9"
    if chars[3] == "?":
        chars[3] = "5"
    if chars[4] == "?":
        chars[4] = "9"
    return "".join(chars)
```

