---
id: 7
title: "Reverse Integer"
difficulty: "בינוני"
source: "LeetCode"
videoUrl: "https://www.youtube.com/embed/gdUy13X_-2g"
---

# Reverse Integer

## תיאור השאלה

Given a signed 32-bit integer `x`, return `x` with its digits reversed. If reversing `x` causes the value to go outside the signed 32-bit integer range `[-2^31, 2^31 - 1]`, then return `0`.

## פיתרון

```javascript
function reverse(x) {
  const INT_MIN = -2147483648;
  const INT_MAX = 2147483647;
  const sign = x < 0 ? -1 : 1;
  let n = Math.abs(x);
  let rev = 0;
  while (n > 0) {
    rev = rev * 10 + (n % 10);
    n = Math.floor(n / 10);
  }
  rev *= sign;
  if (rev < INT_MIN || rev > INT_MAX) return 0;
  return rev;
}
```

```python
def reverse(x: int) -> int:
    INT_MIN = -2**31
    INT_MAX = 2**31 - 1
    sign = -1 if x < 0 else 1
    n = abs(x)
    rev = 0
    while n > 0:
        rev = rev * 10 + (n % 10)
        n //= 10
    rev *= sign
    if rev < INT_MIN or rev > INT_MAX:
        return 0
    return rev
```

## דוגמאות

**Example 1:**
```
Input: x = 123
Output: 321
```

**Example 2:**
```
Input: x = -123
Output: -321
```


